# Allow the proxy to reach private / loopback addresses (e.g. localhost).
# Defaults to true in development and false in production. Set explicitly to
# override. Keep this false (or unset) on a public deployment to prevent SSRF.
# ALLOW_PRIVATE_HOSTS=false
